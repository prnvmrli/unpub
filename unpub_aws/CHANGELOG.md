## 0.2.0

- Update examples and integration docs for `unpub` `3.x` PostgreSQL-based metadata store.
- Keep `unpub_aws` Docker Compose focused on AWS-specific local infra (`mock_s3`).
- Align package dependency constraints with `unpub: ^3.0.0`.

## 0.1.0

- Initial release
